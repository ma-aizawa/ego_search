%a#toTop{:href=>'/'} 使い方に戻る
%h3 結果一覧
%h4 キーワード：#{@query_string}
- unless @result.nil?
  %table{:border=>1}
    %tbody
      - @result.each do |tweet|
        %tr
          %td
            %img{:src=>tweet["profile_image_url"], :width=>48, :height=>48}
          %td=tweet["from_user_name"] |
            <<"<hr class=\"cellHr\" />" |
            <<"<a href='http://twitter.com/#!/#{tweet['from_user']}' target='_blank'>" |
            <<"@"<<tweet["from_user"]<<"</a>" |
          %td.textBody=tweet["text"].gsub(/(http\S+)(\s|$)/, '<a href="\1" target="_blank" >\1</a>\2')

